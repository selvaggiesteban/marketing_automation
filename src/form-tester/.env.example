# Form Tester Environment Configuration
# Copy this file to .env and fill in your values

# SMTP Configuration (required for email fallback)
FORM_TESTER_SMTP_USER=your-email@gmail.com
FORM_TESTER_SMTP_PASSWORD=your-app-password
FORM_TESTER_FROM_EMAIL=sender@example.com

# Note: To customize crawler settings, edit the CONFIGURATION SECTION in main.py

# Optional: Proxy Configuration
# FORM_TESTER_PROXY_URL=socks5://user:pass@host:port
# FORM_TESTER_HTTP_PROXY=http://proxy.example.com:8080
# FORM_TESTER_HTTPS_PROXY=http://proxy.example.com:8080
